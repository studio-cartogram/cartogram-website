import Link from 'next/link';
import styled from 'styled-components';

export const Header = () => {
  return <StyledHeader>Cartogram status</StyledHeader>;
};

const StyledHeader = styled.header`
  text-align: center;
  width: 100%;
`;
